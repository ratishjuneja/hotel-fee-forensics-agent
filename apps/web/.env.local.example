# Copy to .env.local. Base URL of the FeeForensics API (Person A's Fastify server).
# Defaults to http://localhost:4000 if unset. NEXT_PUBLIC_ is required so the
# browser bundle can read it for client-side fetches.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
